<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primeiro</item>
        <item name="colorAccent">@color/primeiro</item>
        <item name="colorPrimaryDark">@color/primeiro</item>
    </style>

</resources>
